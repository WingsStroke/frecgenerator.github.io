import{AppState}from"./state.js";import{cleanNum}from"./math.js";if(typeof Chart!=="undefined"&&typeof ChartBoxPlot!=="undefined"){Chart.register(ChartBoxPlot.BoxPlotController,ChartBoxPlot.BoxAndWiskers)}let activeModalChart=null;const miniChartOptions={responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{x:{display:true},y:{display:true}}};function getChartConfig(ds,type){const labels=ds.classesData.map(c=>cleanNum(c.xi));if(type==="hist")return{data:{labels:labels,datasets:[{type:"bar",label:"Frecuencia fi",data:ds.classesData.map(c=>c.fi),backgroundColor:"rgba(0, 0, 0, 0.1)",borderColor:"#000",borderWidth:1,barPercentage:1,categoryPercentage:1},{type:"line",label:"Polígono",data:ds.classesData.map(c=>c.fi),borderColor:"#000",borderWidth:2,tension:.1,fill:false,pointBackgroundColor:"#000"}]}};if(type==="ojiva")return{type:"line",data:{labels:labels,datasets:[{label:"Ojiva Hi %",data:ds.classesData.map(c=>c.Hi*100),borderColor:"#000",borderWidth:2,fill:true,backgroundColor:"rgba(0, 0, 0, 0.05)",tension:.3}]}};if(type==="box")return{type:"boxplot",data:{labels:["Distribución"],datasets:[{label:ds.name,data:[ds.data],backgroundColor:"rgba(0, 0, 0, 0.1)",borderColor:"#000",borderWidth:2,itemRadius:3,outlierBackgroundColor:"#000"}]}}}function renderChartsForDataset(ds,index){new Chart(document.getElementById(`chartHist-${index}`).getContext("2d"),{...getChartConfig(ds,"hist"),options:{...miniChartOptions,scales:{y:{beginAtZero:true}}}});new Chart(document.getElementById(`chartOjiva-${index}`).getContext("2d"),{...getChartConfig(ds,"ojiva"),options:{...miniChartOptions,scales:{y:{beginAtZero:true,max:100}}}});try{new Chart(document.getElementById(`chartBox-${index}`).getContext("2d"),{...getChartConfig(ds,"box"),options:{...miniChartOptions,indexAxis:"y"}})}catch(e){document.getElementById(`chartBox-${index}`).parentElement.innerHTML=`<p style="color:#a00000; text-align:center; padding:20px;">Error al cargar BoxPlot.</p>`}}window.openChartModal=function(dsIndex,chartType){const ds=AppState.globalDatasets[dsIndex];if(activeModalChart)activeModalChart.destroy();document.getElementById("chartModal").classList.remove("hidden");document.body.classList.add("no-scroll");let titleText="";let options={responsive:true,maintainAspectRatio:false};if(chartType==="hist"){titleText="Histograma y Polígono";options.scales={y:{beginAtZero:true}}}else if(chartType==="ojiva"){titleText="Ojiva Menor que";options.scales={y:{beginAtZero:true,max:100}}}else if(chartType==="box"){titleText="Diagrama de Caja";options.indexAxis="y";options.plugins={legend:{display:false}}}document.getElementById("chartModalTitle").innerText=`${titleText} - ${ds.name}`;activeModalChart=new Chart(document.getElementById("modalCanvas").getContext("2d"),{...getChartConfig(ds,chartType),options:options})};window.toggleAccordion=function(header){header.parentElement.classList.toggle("active")};function createStatRow(label,value,formula){return`<div class="stat-row"><span class="tooltip">${label}<span class="tooltiptext">${formula}</span></span><b>${value}</b></div>`}export function updateCarouselControls(){document.getElementById("carouselIndicator").innerText=`Análisis ${AppState.currentSlide+1} de ${AppState.globalDatasets.length}`;document.getElementById("prevBtn").disabled=AppState.currentSlide===0;document.getElementById("nextBtn").disabled=AppState.currentSlide===AppState.globalDatasets.length-1}export function renderCarousel(){const carousel=document.getElementById("resultsCarousel");carousel.innerHTML="";AppState.currentSlide=0;AppState.globalDatasets.forEach((ds,index)=>{let block=document.createElement("div");block.className="dataset-block";if(ds.type==="bivariate"){let html=`<h3>Análisis Bivariado: ${ds.nameX} (Filas) vs ${ds.nameY} (Columnas)</h3>`;html+=`<div style="overflow-x: auto;"><table class="cross-table"><thead><tr><th>X \\ Y</th>`;ds.uniqueY.forEach(y=>html+=`<th>${y}</th>`);html+=`<th class="total-cell">Total Fila</th></tr></thead><tbody>`;ds.uniqueX.forEach(x=>{html+=`<tr><th>${x}</th>`;ds.uniqueY.forEach(y=>{html+=`<td>${ds.matrix[x][y]}</td>`});html+=`<td class="total-cell">${ds.rowTotals[x]}</td></tr>`});html+=`<tr><th class="total-cell">Total Columna</th>`;ds.uniqueY.forEach(y=>html+=`<td class="total-cell">${ds.colTotals[y]}</td>`);html+=`<td class="total-cell" style="background:#333; color:#fff;">${ds.grandTotal}</td></tr></tbody></table></div>`;html+=`
            <div class="stats-grid">
                <div class="stat-card" style="grid-column: 1 / -1; border-left: 4px solid #a00000;">
                    <h3 style="color:#a00000;">Analista Automático</h3>
                    <p style="font-size:14px; line-height:1.6;">Se cruzaron un total de <b>${ds.grandTotal} registros válidos</b>. La matriz muestra la frecuencia conjunta de los datos. Observa las celdas con mayores valores para identificar la intersección más fuerte entre la variable <i>${ds.nameX}</i> y la variable <i>${ds.nameY}</i>. El gráfico de barras apiladas a continuación te ayudará a visualizar qué categoría de la columna predomina dentro de cada categoría de la fila.</p>
                </div>
            </div>`;html+=`<h3 style="margin-top:30px; border-bottom:1px solid #ccc; padding-bottom:5px;">Gráfico de Barras Apiladas (Composición)</h3>`;html+=`<div class="chart-canvas-wrapper" style="height:400px; margin-bottom:40px;"><canvas id="chartBivariate-${index}"></canvas></div>`;block.innerHTML=html;carousel.appendChild(block);setTimeout(()=>{const ctx=document.getElementById(`chartBivariate-${index}`).getContext("2d");const colors=["#a00000","#0000a0","#00a000","#e6a800","#660066","#006666","#555555","#ff6600"];const datasets=ds.uniqueY.map((y,i)=>{return{label:String(y),data:ds.uniqueX.map(x=>ds.matrix[x][y]),backgroundColor:colors[i%colors.length]}});new Chart(ctx,{type:"bar",data:{labels:ds.uniqueX.map(String),datasets:datasets},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:true,position:"bottom"}},scales:{x:{stacked:true},y:{stacked:true}}}})},0)}else{let kFormula=AppState.activeMethod==="sturges"?"k ≈ 1 + 3.322 · log₁₀(n)":"Manual";let methodLabel=AppState.activeMethod==="sturges"?" - Sturges":" - Manual";let freqHtml=`<h3>Análisis ${index+1}: ${ds.name} <span style="font-size:13px; font-weight:normal; color:#666;">(${ds.isGrouped?"Datos Agrupados":"Frecuencias Simples"})</span></h3><table><thead><tr>`;if(ds.isGrouped){freqHtml+=`<th>Límite Inf. (Li)</th><th>Límite Sup. (Ls)</th><th>Marca de Clase (Xi)</th>`}else{freqHtml+=`<th>Dato (Xi)</th>`}freqHtml+=`<th>Frec. Abs. (fi)</th><th>Frec. Acum. (Fi)</th><th>Frec. Rel. (hi)</th><th>Frec. Rel. Acum. (Hi)</th></tr></thead><tbody>`;ds.classesData.forEach(c=>{freqHtml+=`<tr>`;if(ds.isGrouped){freqHtml+=`<td>${cleanNum(c.min)}</td><td>${cleanNum(c.max)}</td>`}freqHtml+=`<td>${cleanNum(c.xi)}</td><td>${c.fi}</td><td>${c.Fi}</td><td>${cleanNum(c.hi)}</td><td>${cleanNum(c.Hi)}</td></tr>`});freqHtml+=`</tbody></table>`;let statsHtml=`<div class="stats-grid">
                <div class="stat-card">
                    <h3>Parámetros Base</h3>
                    ${createStatRow("Mínimo:",cleanNum(ds.minVal),"min(xᵢ)")}
                    ${createStatRow("Máximo:",cleanNum(ds.maxVal),"max(xᵢ)")}
                    ${ds.isGrouped?createStatRow(`Intervalos k${methodLabel}:`,ds.numClasses,kFormula):createStatRow("Valores Únicos:",ds.uniqueValsCount,"No agrupados")}
                    ${ds.isGrouped?createStatRow("Amplitud A:",cleanNum(ds.amplitude),"A = Rango / k"):""}
                </div>
                <div class="stat-card">
                    <h3>Tendencia Central</h3>
                    ${createStatRow("Media Aritmética:",cleanNum(ds.stats.mean),"x̄ = (Σxᵢ) / n")}
                    ${createStatRow("Media Geométrica:",cleanNum(ds.stats.geoMean),"MG = ⁿ√(x₁···xₙ)")}
                    ${createStatRow("Media Armónica:",cleanNum(ds.stats.harMean),"MH = n / Σ(1/xᵢ)")}
                    ${createStatRow("Mediana:",cleanNum(ds.stats.median),ds.isGrouped?"Me = Lᵢ + A·[(n/2 - Fᵢ₋₁)/fᵢ]":"Valor central")}
                    ${createStatRow("Moda:",ds.stats.mode.map(m=>cleanNum(m)).join(","),ds.isGrouped?"Mo = Lᵢ + A·[(fᵢ - fᵢ₋₁)/(2fᵢ - fᵢ₋₁ - fᵢ₊₁)]":"Valor más repetido")}
                </div>
                <div class="stat-card">
                    <h3>Dispersión y Forma</h3>
                    ${createStatRow("Rango:",cleanNum(ds.range),"R = x_max - x_min")}
                    ${createStatRow("Varianza:",cleanNum(ds.stats.variance),"s² = Σ(xᵢ - x̄)² / (n - 1)")}
                    ${createStatRow("Desviación Estándar:",cleanNum(ds.stats.stdDev),"s = √s²")}
                    ${createStatRow("Coeficiente Variación:",cleanNum(ds.stats.cv,2)+"%","CV = (s/x̄)·100%")}
                    ${createStatRow("Asimetría:",cleanNum(ds.stats.skewness),"As = [n/((n-1)(n-2))] · Σ[(xᵢ-x̄)/s]³")}
                </div>
                <div class="stat-card">
                    <h3>Posición</h3>
                    ${createStatRow("Percentil 10:",cleanNum(ds.stats.p10),"P₁₀ = Lᵢ + A·[(10n/100 - Fᵢ₋₁)/fᵢ]")}
                    ${createStatRow("Cuartil 1:",cleanNum(ds.stats.q1),"Q₁ = Lᵢ + A·[(n/4 - Fᵢ₋₁)/fᵢ]")}
                    ${createStatRow("Cuartil 2:",cleanNum(ds.stats.q2),"Q₂ = Mediana")}
                    ${createStatRow("Cuartil 3:",cleanNum(ds.stats.q3),"Q₃ = Lᵢ + A·[(3n/4 - Fᵢ₋₁)/fᵢ]")}
                    ${createStatRow("Percentil 90:",cleanNum(ds.stats.p90),"P₉₀ = Lᵢ + A·[(90n/100 - Fᵢ₋₁)/fᵢ]")}
                </div>
            </div>`;let chartsHtml=`<div class="chart-accordion"><div class="accordion-item"><div class="accordion-header" onclick="toggleAccordion(this)"><span>Gráficos Estadísticos - Clic para ampliar</span><span class="accordion-arrow">▼</span></div><div class="accordion-content"><div class="chart-grid"><div class="chart-item" onclick="openChartModal(${index}, 'hist')"><h4>Histograma</h4><div class="chart-canvas-wrapper"><canvas id="chartHist-${index}"></canvas></div></div><div class="chart-item" onclick="openChartModal(${index}, 'ojiva')"><h4>Ojiva</h4><div class="chart-canvas-wrapper"><canvas id="chartOjiva-${index}"></canvas></div></div><div class="chart-item" onclick="openChartModal(${index}, 'box')"><h4>Caja y Bigotes</h4><div class="chart-canvas-wrapper"><canvas id="chartBox-${index}"></canvas></div></div></div></div></div></div>`;block.innerHTML=freqHtml+statsHtml+chartsHtml;carousel.appendChild(block);setTimeout(()=>renderChartsForDataset(ds,index),0)}});document.getElementById("resultsArea").classList.remove("hidden");document.getElementById("exportBtn").classList.remove("hidden");document.getElementById("exportPdfBtn").classList.remove("hidden");if(AppState.analysisMode==="bivariate"){document.getElementById("floatingProcedureBtn").classList.add("hidden")}else{document.getElementById("floatingProcedureBtn").classList.remove("hidden")}carousel.scrollLeft=0;updateCarouselControls()}let preview2DArray=[];let isDragging=false;let startCell=null;let lastClickedCell=null;let autoScrollInterval=null;let mobileLongPressTimer=null;let mobileRangeStartCell=null;let isMobileSelectionMode=false;export function openExcelModal(fileId){AppState.currentPreviewFileId=fileId;const fileObj=AppState.uploadedFilesMap.get(fileId);document.getElementById("modalTitle").innerText=`Previsualización: ${fileObj.file.name}`;updateRangeCount();const reader=new FileReader;reader.onload=e=>{const workbook=XLSX.read(new Uint8Array(e.target.result),{type:"array"});preview2DArray=XLSX.utils.sheet_to_json(workbook.Sheets[workbook.SheetNames[0]],{header:1,defval:""});renderPreviewTable();if(fileObj.customRanges.length>0)alert(`Este archivo ya tiene ${fileObj.customRanges.length} rango(s) guardado(s).`);document.getElementById("previewModal").classList.remove("hidden");document.body.classList.add("no-scroll")};reader.readAsArrayBuffer(fileObj.file)}function renderPreviewTable(){const container=document.getElementById("tableContainer");let html='<table id="interactiveTable">';preview2DArray.forEach((row,rIdx)=>{html+="<tr>";row.forEach((cell,cIdx)=>html+=`<td data-r="${rIdx}" data-c="${cIdx}">${cell!==undefined?cell:""}</td>`);html+="</tr>"});container.innerHTML=html+"</table>";const table=document.getElementById("interactiveTable");table.addEventListener("mousedown",e=>{if(e.target.tagName!=="TD"||e.target.classList.contains("cell-saved")||e.button!==0)return;isDragging=true;const r=parseInt(e.target.dataset.r),c=parseInt(e.target.dataset.c);if(e.ctrlKey||e.metaKey){e.target.classList.toggle("cell-selected");lastClickedCell={r:r,c:c};startCell=null}else if(e.shiftKey&&lastClickedCell){selectRange(lastClickedCell,{r:r,c:c},true)}else{clearSelection();e.target.classList.add("cell-selected");startCell={r:r,c:c};lastClickedCell={r:r,c:c}}});table.addEventListener("mouseover",e=>{if(!isDragging||!startCell||e.target.tagName!=="TD"||e.target.classList.contains("cell-saved"))return;selectRange(startCell,{r:parseInt(e.target.dataset.r),c:parseInt(e.target.dataset.c)},true)});table.addEventListener("touchstart",e=>{if(e.target.tagName!=="TD"||e.target.classList.contains("cell-saved"))return;const r=parseInt(e.target.dataset.r),c=parseInt(e.target.dataset.c);mobileLongPressTimer=setTimeout(()=>{if(isMobileSelectionMode&&mobileRangeStartCell){selectRange(mobileRangeStartCell,{r:r,c:c},true);isMobileSelectionMode=false;mobileRangeStartCell=null;document.querySelectorAll(".mobile-range-start").forEach(el=>el.classList.remove("mobile-range-start"));if(navigator.vibrate)navigator.vibrate(50)}else{isMobileSelectionMode=true;mobileRangeStartCell={r:r,c:c};e.target.classList.add("mobile-range-start");if(navigator.vibrate)navigator.vibrate(50)}},1e3)},{passive:true});table.addEventListener("touchend",()=>clearTimeout(mobileLongPressTimer));table.addEventListener("touchmove",()=>clearTimeout(mobileLongPressTimer),{passive:true});table.addEventListener("touchcancel",()=>clearTimeout(mobileLongPressTimer));container.addEventListener("mousemove",e=>{if(!isDragging)return;const rect=container.getBoundingClientRect();const buffer=40;let dx=0,dy=0;if(e.clientX<rect.left+buffer)dx=-15;else if(e.clientX>rect.right-buffer)dx=15;if(e.clientY<rect.top+buffer)dy=-15;else if(e.clientY>rect.bottom-buffer)dy=15;if(dx!==0||dy!==0){if(!autoScrollInterval)autoScrollInterval=setInterval(()=>{container.scrollLeft+=dx;container.scrollTop+=dy},30)}else{clearInterval(autoScrollInterval);autoScrollInterval=null}});window.addEventListener("mouseup",()=>{isDragging=false;clearInterval(autoScrollInterval);autoScrollInterval=null})}function selectRange(start,end,clearFirst){if(clearFirst)clearSelection();const minR=Math.min(start.r,end.r),maxR=Math.max(start.r,end.r);const minC=Math.min(start.c,end.c),maxC=Math.max(start.c,end.c);for(let r=minR;r<=maxR;r++)for(let c=minC;c<=maxC;c++){const td=document.querySelector(`#interactiveTable td[data-r="${r}"][data-c="${c}"]`);if(td&&!td.classList.contains("cell-saved"))td.classList.add("cell-selected")}}function clearSelection(){document.querySelectorAll("#interactiveTable .cell-selected").forEach(td=>td.classList.remove("cell-selected"));document.querySelectorAll("#interactiveTable .mobile-range-start").forEach(td=>td.classList.remove("mobile-range-start"));isMobileSelectionMode=false;mobileRangeStartCell=null}export function updateRangeCount(){document.getElementById("rangeCount").innerText=`Rangos guardados: ${AppState.uploadedFilesMap.get(AppState.currentPreviewFileId).customRanges.length}/10`}let bivariate2DArray=[];let isBivDragging=false;let startBivCell=null;let lastBivClickedCell=null;let bivAutoScrollInterval=null;let bivMobileLongPressTimer=null;let bivMobileRangeStartCell=null;let bivIsMobileSelectionMode=false;export function openBivariateModal(fileId){AppState.currentPreviewFileId=fileId;AppState.tempBivariateX=[];AppState.tempBivariateY=[];document.getElementById("countX").innerText="Datos seleccionados: 0";document.getElementById("countY").innerText="Datos seleccionados: 0";document.getElementById("bivariateNameX").value="";document.getElementById("bivariateNameY").value="";const fileObj=AppState.uploadedFilesMap.get(fileId);document.getElementById("bivariateModalTitle").innerText=`Configurar Cruce Bivariado: ${fileObj.file.name}`;updateBivariateRangeCount();const reader=new FileReader;reader.onload=e=>{const workbook=XLSX.read(new Uint8Array(e.target.result),{type:"array"});bivariate2DArray=XLSX.utils.sheet_to_json(workbook.Sheets[workbook.SheetNames[0]],{header:1,defval:""});renderBivariateTable();document.getElementById("bivariateModal").classList.remove("hidden");document.body.classList.add("no-scroll")};reader.readAsArrayBuffer(fileObj.file)}function renderBivariateTable(){const container=document.getElementById("bivariateTableContainer");let html='<table id="interactiveBivariateTable">';bivariate2DArray.forEach((row,rIdx)=>{html+="<tr>";row.forEach((cell,cIdx)=>html+=`<td data-r="${rIdx}" data-c="${cIdx}">${cell!==undefined?cell:""}</td>`);html+="</tr>"});container.innerHTML=html+"</table>";const table=document.getElementById("interactiveBivariateTable");table.addEventListener("contextmenu",e=>{if(e.altKey)e.preventDefault()});table.addEventListener("mousedown",e=>{if(e.target.tagName!=="TD"||e.target.classList.contains("cell-saved-x")||e.target.classList.contains("cell-saved-y"))return;if(e.altKey){e.preventDefault();if(e.button===0){document.getElementById("bivariateNameX").value=e.target.innerText.trim()}else if(e.button===2){document.getElementById("bivariateNameY").value=e.target.innerText.trim()}return}if(e.button!==0)return;isBivDragging=true;const r=parseInt(e.target.dataset.r),c=parseInt(e.target.dataset.c);if(e.ctrlKey||e.metaKey){e.target.classList.toggle("cell-selected");lastBivClickedCell={r:r,c:c};startBivCell=null}else if(e.shiftKey&&lastBivClickedCell){selectBivRange(lastBivClickedCell,{r:r,c:c},true)}else{clearBivariateSelection();e.target.classList.add("cell-selected");startBivCell={r:r,c:c};lastBivClickedCell={r:r,c:c}}});table.addEventListener("mouseover",e=>{if(!isBivDragging||!startBivCell||e.target.tagName!=="TD"||e.target.classList.contains("cell-saved-x")||e.target.classList.contains("cell-saved-y"))return;selectBivRange(startBivCell,{r:parseInt(e.target.dataset.r),c:parseInt(e.target.dataset.c)},true)});table.addEventListener("touchstart",e=>{if(e.target.tagName!=="TD"||e.target.classList.contains("cell-saved-x")||e.target.classList.contains("cell-saved-y"))return;const r=parseInt(e.target.dataset.r),c=parseInt(e.target.dataset.c);bivMobileLongPressTimer=setTimeout(()=>{if(bivIsMobileSelectionMode&&bivMobileRangeStartCell){selectBivRange(bivMobileRangeStartCell,{r:r,c:c},true);bivIsMobileSelectionMode=false;bivMobileRangeStartCell=null;document.querySelectorAll(".mobile-range-start").forEach(el=>el.classList.remove("mobile-range-start"));if(navigator.vibrate)navigator.vibrate(50)}else{bivIsMobileSelectionMode=true;bivMobileRangeStartCell={r:r,c:c};e.target.classList.add("mobile-range-start");if(navigator.vibrate)navigator.vibrate(50)}},1e3)},{passive:true});table.addEventListener("touchend",()=>clearTimeout(bivMobileLongPressTimer));table.addEventListener("touchmove",()=>clearTimeout(bivMobileLongPressTimer),{passive:true});table.addEventListener("touchcancel",()=>clearTimeout(bivMobileLongPressTimer));container.addEventListener("mousemove",e=>{if(!isBivDragging)return;const rect=container.getBoundingClientRect();const buffer=40;let dx=0,dy=0;if(e.clientX<rect.left+buffer)dx=-15;else if(e.clientX>rect.right-buffer)dx=15;if(e.clientY<rect.top+buffer)dy=-15;else if(e.clientY>rect.bottom-buffer)dy=15;if(dx!==0||dy!==0){if(!bivAutoScrollInterval)bivAutoScrollInterval=setInterval(()=>{container.scrollLeft+=dx;container.scrollTop+=dy},30)}else{clearInterval(bivAutoScrollInterval);bivAutoScrollInterval=null}});window.addEventListener("mouseup",()=>{isBivDragging=false;clearInterval(bivAutoScrollInterval);bivAutoScrollInterval=null})}function selectBivRange(start,end,clearFirst){if(clearFirst)clearBivariateSelection();const minR=Math.min(start.r,end.r),maxR=Math.max(start.r,end.r);const minC=Math.min(start.c,end.c),maxC=Math.max(start.c,end.c);for(let r=minR;r<=maxR;r++)for(let c=minC;c<=maxC;c++){const td=document.querySelector(`#interactiveBivariateTable td[data-r="${r}"][data-c="${c}"]`);if(td&&!td.classList.contains("cell-saved-x")&&!td.classList.contains("cell-saved-y"))td.classList.add("cell-selected")}}function clearBivariateSelection(){document.querySelectorAll("#interactiveBivariateTable .cell-selected").forEach(td=>td.classList.remove("cell-selected"));document.querySelectorAll("#interactiveBivariateTable .mobile-range-start").forEach(td=>td.classList.remove("mobile-range-start"));bivIsMobileSelectionMode=false;bivMobileRangeStartCell=null}function updateBivariateRangeCount(){document.getElementById("bivariateRangeCount").innerText=`Cruces guardados: ${AppState.uploadedFilesMap.get(AppState.currentPreviewFileId).customRanges.length}/10`}export function initUIListeners(){window.addEventListener("keydown",e=>{if((e.ctrlKey||e.metaKey)&&e.shiftKey&&e.key==="ArrowDown"){const uniModal=document.getElementById("previewModal");const bivModal=document.getElementById("bivariateModal");if(!uniModal.classList.contains("hidden")&&lastClickedCell){e.preventDefault();let endCell={r:preview2DArray.length-1,c:lastClickedCell.c};let start=startCell||lastClickedCell;selectRange(start,endCell,true);lastClickedCell=endCell}else if(!bivModal.classList.contains("hidden")&&lastBivClickedCell){e.preventDefault();let endCell={r:bivariate2DArray.length-1,c:lastBivClickedCell.c};let start=startBivCell||lastBivClickedCell;selectBivRange(start,endCell,true);lastBivClickedCell=endCell}}});document.getElementById("prevBtn").addEventListener("click",()=>{if(AppState.currentSlide>0){AppState.currentSlide--;document.getElementById("resultsCarousel").scrollTo({left:document.getElementById("resultsCarousel").clientWidth*AppState.currentSlide,behavior:"smooth"});updateCarouselControls()}});document.getElementById("nextBtn").addEventListener("click",()=>{if(AppState.currentSlide<AppState.globalDatasets.length-1){AppState.currentSlide++;document.getElementById("resultsCarousel").scrollTo({left:document.getElementById("resultsCarousel").clientWidth*AppState.currentSlide,behavior:"smooth"});updateCarouselControls()}});document.getElementById("resultsCarousel").addEventListener("scroll",e=>{const newSlide=Math.round(e.target.scrollLeft/e.target.clientWidth);if(newSlide!==AppState.currentSlide&&newSlide>=0&&newSlide<AppState.globalDatasets.length){AppState.currentSlide=newSlide;updateCarouselControls()}});document.getElementById("closeModalBtn").addEventListener("click",()=>{document.getElementById("previewModal").classList.add("hidden");document.body.classList.remove("no-scroll");clearSelection()});document.getElementById("closeChartModalBtn").addEventListener("click",()=>{document.getElementById("chartModal").classList.add("hidden");document.body.classList.remove("no-scroll")});document.getElementById("clearSelectionBtn").addEventListener("click",clearSelection);document.getElementById("resetRangesBtn").addEventListener("click",()=>{if(!confirm("¿Borrar todos los rangos guardados para este archivo?"))return;AppState.uploadedFilesMap.get(AppState.currentPreviewFileId).customRanges=[];document.querySelectorAll("#interactiveTable .cell-saved").forEach(td=>td.classList.remove("cell-saved"));updateRangeCount()});document.getElementById("saveRangeBtn").addEventListener("click",()=>{const fileObj=AppState.uploadedFilesMap.get(AppState.currentPreviewFileId);if(fileObj.customRanges.length>=AppState.MAX_DATASETS)return alert("Máximo de rangos alcanzado para este archivo.");const selected=document.querySelectorAll("#interactiveTable .cell-selected");if(selected.length===0)return alert("Selecciona datos primero.");let nums=[];selected.forEach(td=>{let val=parseFloat(td.innerText);if(!isNaN(val))nums.push(val);td.classList.remove("cell-selected");td.classList.add("cell-saved")});if(nums.length===0)return alert("No hay números válidos en tu selección.");fileObj.customRanges.push(nums);updateRangeCount()});document.getElementById("finishRangesBtn").addEventListener("click",()=>{document.getElementById("previewModal").classList.add("hidden");document.body.classList.remove("no-scroll")});document.getElementById("closeBivariateModalBtn").addEventListener("click",()=>{document.getElementById("bivariateModal").classList.add("hidden");document.body.classList.remove("no-scroll");clearBivariateSelection()});document.getElementById("clearBivariateSelectionBtn").addEventListener("click",clearBivariateSelection);document.getElementById("setRangeXBtn").addEventListener("click",()=>{const selected=document.querySelectorAll("#interactiveBivariateTable .cell-selected");if(selected.length===0)return alert("Selecciona celdas en la tabla primero.");let temp=[];selected.forEach(td=>{let val=td.innerText.trim();if(val!==""){temp.push(val);td.classList.remove("cell-selected");td.classList.add("cell-saved-x")}});AppState.tempBivariateX=temp;document.getElementById("countX").innerText=`Datos seleccionados: ${temp.length}`});document.getElementById("setRangeYBtn").addEventListener("click",()=>{const selected=document.querySelectorAll("#interactiveBivariateTable .cell-selected");if(selected.length===0)return alert("Selecciona celdas en la tabla primero.");let temp=[];selected.forEach(td=>{let val=td.innerText.trim();if(val!==""){temp.push(val);td.classList.remove("cell-selected");td.classList.add("cell-saved-y")}});AppState.tempBivariateY=temp;document.getElementById("countY").innerText=`Datos seleccionados: ${temp.length}`});document.getElementById("resetBivariateBtn").addEventListener("click",()=>{if(!confirm("¿Borrar todos los cruces guardados para este archivo?"))return;AppState.uploadedFilesMap.get(AppState.currentPreviewFileId).customRanges=[];document.querySelectorAll("#interactiveBivariateTable .cell-saved-x").forEach(td=>td.classList.remove("cell-saved-x"));document.querySelectorAll("#interactiveBivariateTable .cell-saved-y").forEach(td=>td.classList.remove("cell-saved-y"));AppState.tempBivariateX=[];AppState.tempBivariateY=[];document.getElementById("countX").innerText="Datos seleccionados: 0";document.getElementById("countY").innerText="Datos seleccionados: 0";updateBivariateRangeCount()});document.getElementById("saveBivariateBtn").addEventListener("click",()=>{if(AppState.tempBivariateX.length===0||AppState.tempBivariateY.length===0){return alert("Debes asignar datos tanto para la Variable X como para la Variable Y.")}if(AppState.tempBivariateX.length!==AppState.tempBivariateY.length){return alert(`Los rangos no coinciden en tamaño. Has asignado ${AppState.tempBivariateX.length} datos a X, pero ${AppState.tempBivariateY.length} datos a Y.`)}const nameX=document.getElementById("bivariateNameX").value.trim()||"Variable X";const nameY=document.getElementById("bivariateNameY").value.trim()||"Variable Y";const fileObj=AppState.uploadedFilesMap.get(AppState.currentPreviewFileId);fileObj.customRanges.push({type:"bivariate_pair",colX:{header:nameX,data:[...AppState.tempBivariateX]},colY:{header:nameY,data:[...AppState.tempBivariateY]}});updateBivariateRangeCount();AppState.tempBivariateX=[];AppState.tempBivariateY=[];document.getElementById("countX").innerText="Datos seleccionados: 0";document.getElementById("countY").innerText="Datos seleccionados: 0";document.getElementById("bivariateNameX").value="";document.getElementById("bivariateNameY").value="";alert(`Cruce guardado exitosamente.\n\nPresiona 'Procesar Datos' para ver los resultados.`);document.getElementById("bivariateModal").classList.add("hidden");document.body.classList.remove("no-scroll")});document.getElementById("floatingProcedureBtn").addEventListener("click",()=>{const ds=AppState.globalDatasets[AppState.currentSlide];if(!ds||ds.type==="bivariate")return;document.getElementById("procedureModalTitle").innerText=`Procedimiento Completo: ${ds.name}`;let isSturges=AppState.activeMethod==="sturges";let firstRow=ds.classesData[0]||{};let html=`
            <div class="procedure-step">
                <h3>1. Parámetros Base para la Tabla</h3>
                <div class="math-formula">Rango R = ${cleanNum(ds.maxVal)} - ${cleanNum(ds.minVal)} = <span class="math-highlight">${cleanNum(ds.range)}</span></div>`;if(ds.isGrouped){html+=`<div class="math-formula">Intervalos k ${isSturges?`≈ 1 + 3.322 * log₁₀(${ds.n}) = `:"Manual = "} <span class="math-highlight">${ds.numClasses}</span></div>
                     <div class="math-formula">Amplitud A = ${cleanNum(ds.range)} / ${ds.numClasses} = <span class="math-highlight">${cleanNum(ds.amplitude)}</span></div></div>
                     <div class="procedure-step">
                        <h3>2. Construcción de la Tabla (Ejemplo 1ra Fila)</h3>
                        <div class="math-formula">Límite Inferior Li = Mínimo = <span class="math-highlight">${cleanNum(firstRow.min)}</span></div>
                        <div class="math-formula">Límite Superior Ls = Li + A = ${cleanNum(firstRow.min)} + ${cleanNum(ds.amplitude)} = <span class="math-highlight">${cleanNum(firstRow.max)}</span></div>
                        <div class="math-formula">Marca de Clase Xi = (Li + Ls) / 2 = <span class="math-highlight">${cleanNum(firstRow.xi)}</span></div>`}else{html+=`<p style="margin-top:10px;"><i>Has seleccionado generar una tabla de Frecuencias Simples (datos no agrupados). Se ha omitido el cálculo de intervalos.</i></p></div>
                     <div class="procedure-step">
                        <h3>2. Construcción de la Tabla (Ejemplo 1ra Fila)</h3>
                        <div class="math-formula">Dato Xi = El valor único detectado = <span class="math-highlight">${cleanNum(firstRow.xi)}</span></div>`}html+=`
                <div class="math-formula">Frec. Absoluta fi = Recuento de veces que aparece = <span class="math-highlight">${firstRow.fi}</span></div>
                <div class="math-formula">Frec. Acumulada Fi = Suma sucesiva de frecuencias = <span class="math-highlight">${firstRow.Fi}</span></div>
                <div class="math-formula">Frec. Relativa hi = fi / n = ${firstRow.fi} / ${ds.n} = <span class="math-highlight">${cleanNum(firstRow.hi)}</span></div>
                <div class="math-formula">Frec. Rel. Acumulada Hi = Fi / n = <span class="math-highlight">${cleanNum(firstRow.Hi)}</span></div>
            </div>

            <div class="procedure-step">
                <h3>3. Medidas de Tendencia Central</h3>
                <div class="math-formula">Media Aritmética x̄ = ${cleanNum(ds.stats.sum)} / ${ds.n} = <span class="math-highlight">${cleanNum(ds.stats.mean)}</span></div>
                <div class="math-formula">Media Geométrica MG = ⁿ√(x₁···xₙ) = <span class="math-highlight">${cleanNum(ds.stats.geoMean)}</span></div>
                <div class="math-formula">Media Armónica MH = n / Σ(1/xᵢ) = <span class="math-highlight">${cleanNum(ds.stats.harMean)}</span></div>
                <div class="math-formula">Mediana Me = Posición central de los datos ordenados = <span class="math-highlight">${cleanNum(ds.stats.median)}</span></div>
                <div class="math-formula">Moda Mo = Valor o valores más frecuentes = <span class="math-highlight">${ds.stats.mode.map(m=>cleanNum(m)).join(", ")}</span></div>
            </div>
            
            <div class="procedure-step">
                <h3>4. Medidas de Posición</h3>
                <div class="math-formula">Percentil 10 P₁₀ = <span class="math-highlight">${cleanNum(ds.stats.p10)}</span></div>
                <div class="math-formula">Cuartil 1 Q₁ = <span class="math-highlight">${cleanNum(ds.stats.q1)}</span></div>
                <div class="math-formula">Cuartil 2 Q₂ (Mediana) = <span class="math-highlight">${cleanNum(ds.stats.q2)}</span></div>
                <div class="math-formula">Cuartil 3 Q₃ = <span class="math-highlight">${cleanNum(ds.stats.q3)}</span></div>
                <div class="math-formula">Percentil 90 P₉₀ = <span class="math-highlight">${cleanNum(ds.stats.p90)}</span></div>
            </div>

            <div class="procedure-step">
                <h3>5. Medidas de Dispersión y Forma</h3>
                <div class="math-formula">Varianza s² = ${cleanNum(ds.stats.varianceSum)} / (${ds.n} - 1) = <span class="math-highlight">${cleanNum(ds.stats.variance)}</span></div>
                <div class="math-formula">Desv. Estándar s = √${cleanNum(ds.stats.variance)} = <span class="math-highlight">${cleanNum(ds.stats.stdDev)}</span></div>
                <div class="math-formula">C. Variación CV = (${cleanNum(ds.stats.stdDev)} / ${cleanNum(ds.stats.mean)}) * 100% = <span class="math-highlight">${cleanNum(ds.stats.cv,2)}%</span></div>
                <div class="math-formula">Asimetría As = [n/((n-1)(n-2))] · Σ[(xᵢ-x̄)/s]³ = <span class="math-highlight">${cleanNum(ds.stats.skewness)}</span></div>
            </div>
        `;document.getElementById("procedureContent").innerHTML=html;document.getElementById("procedureModal").classList.remove("hidden");document.body.classList.add("no-scroll")});document.getElementById("closeProcedureModalBtn").addEventListener("click",()=>{document.getElementById("procedureModal").classList.add("hidden");document.body.classList.remove("no-scroll")})}